CHIP A6Q2 {
    IN N, L, K;
    OUT H;
    PARTS:
    Not(in = L, out = notL);
    And(a = N, b = notL, out = and1);
	Or(a = N, b = notL, out = or1);
	Or(a = or1, b = and1, out = or2);
    And(a = or2, b = K, out = H);
}
