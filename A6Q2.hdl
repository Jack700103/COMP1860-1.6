CHIP A6Q2 {
    IN N, L, K;
    OUT H;
    PARTS:
        Not(in = L, out = l_not);
        And(a = N, b = l_not, out = and_result_1);
        And(a = N, b = K, out = and_result_2);
        Or(a = and_result_1, b = and_result_2, out = or_result);
        And(a = or_result, b = K, out = H);
}
