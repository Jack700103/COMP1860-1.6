CHIP A6Q2 {
    IN N, L, K;
    OUT H;
    PARTS:
    Not(in=L, out=l_not);
    And(a=N, b=l_not, out=and1);
    And(a=N, b=K, out=and2);
    Or(a=and1, b=and2, out=or1);
    And(a=or1, b=K, out=H);
}
